# fast_net

## Getting Started

Clone the repo with submodules:

```bash
git clone --recurse-submodules git@github.com:sssemil/fast_net.git 
```

Build:

```bash 
mkdir build
cd build
cmake ..
make
```